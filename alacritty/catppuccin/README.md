# Catppuccin for Alacritty

Flavour files from [catppuccin/alacritty](https://github.com/catppuccin/alacritty).
Only the flavours in use are kept here: `frappe` (active, imported by
`../alacritty.toml`) and `mocha` (dark alternate — swap the import to use it).
